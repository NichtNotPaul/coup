# coup

run ``pip install -r requirements.txt`` to install all needed libarys.

before inserting your discord token make sure to run ``cp config_template.py config.py`` and then use the newly created config.py:
